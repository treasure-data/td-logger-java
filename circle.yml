machine:
  java:
    version: oraclejdk6

test:
  override:
    - mvn test -P core
    - cp -R build/reports/* $CIRCLE_ARTIFACTS

notify:
  webhooks:
    - url: http://td-beda.herokuapp.com/circleci_callback
